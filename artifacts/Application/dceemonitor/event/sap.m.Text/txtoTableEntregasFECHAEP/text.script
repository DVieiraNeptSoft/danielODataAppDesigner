if (typeof FECHAEP === "undefined" || FECHAEP === null || FECHAEP === "") { return; }  
var formattedText = FECHAEP; 
formattedText = (formattedText.replace(/-/g, ".")).substr(0,10);
return formattedText;