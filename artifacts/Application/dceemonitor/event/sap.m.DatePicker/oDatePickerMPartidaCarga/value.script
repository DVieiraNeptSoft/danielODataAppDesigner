if (typeof FECHAPC === "undefined" || FECHAPC === null || FECHAPC === "") { return; }  
var formattedText = FECHAPC; 
formattedText = (FECHAPC.replace(/-/g, ".")).substr(0,10);
return formattedText;