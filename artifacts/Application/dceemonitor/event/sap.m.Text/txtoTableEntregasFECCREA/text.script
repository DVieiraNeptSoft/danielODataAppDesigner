if (typeof FECCREA === "undefined" || FECCREA === null || FECCREA === "") { return; }  
var formattedText = FECCREA; 
formattedText = (formattedText.replace(/-/g, ".")).substr(0,10);
return formattedText;